package com.github.yohohaha.algorithms;

import com.github.yohohaha.algorithms.commons.BinaryTreeNode;
import org.junit.Test;

import java.util.ArrayDeque;
import java.util.Deque;
import java.util.Iterator;

import static com.github.yohohaha.algorithms.学点算法十二_将数组转为二叉树.convertArray2Tree;

/**
 * created at 2020/08/03 16:35:01
 *
 * @author Yohohaha
 */
public class 学点算法十三_二叉树打印美化算法 {

    @Test
    public void testReverseOrder() {

    }

    public static <E extends Comparable<E>> void printBeautifulBinaryTree(BinaryTreeNode<E> root) {
        // 定义元素间间隔nodeInterval
        // 定义树层级间隔levelInterval
        // 获取高度h和最大的元素宽度maxNodeLen

        // 根据上述两个条件获取画布长和宽
        // 长为(maxNodeLen + nodeInterval) * 2 ^ (h-1)
        // 宽为(levelInterval + 1) * (h - 1) + 1




    }
}
